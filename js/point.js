class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
    this.color = '#a7c7e7';
    this.stroke = '#888';
    this.anchor = {
      x: x,
      y: y,
      color: '#a7c7e7',
      stroke: '#888'
    }
  }
}